# DatabaseFactoryDemo
Demo of Factory Design Pattern in .NET with EF Core providers (SQL Server, SQLite, PostgreSQL).


## How to run
1. Set connection strings in `appsettings.json`.
2. Build & run migrations for a target context.
3. Run the console app and pick a DB.
